======================================================================================
Olist 真实数据四级对账   （库=sales_analysis@127.0.0.1:3306）
======================================================================================

【1】原始层（Olist 发表的数据）
  items 行数            = 112650
  orders 行数           = 99441
  items 中出现的订单数  = 98666
  Σ price（商品金额）   = 13591643.70 BRL
  Σ freight（运费）     = 2251909.54 BRL
  Σ payment_value（实收，含运费/分期，口径不同，仅作对照）= 16008872.12 BRL

【2】转换后 ODS CSV（com.sales.generator.RealDataImporter 产出）
  明细行数              = 102425
  Σ pay_amount          = 13591643.70 BRL
  按 (order_id, product_id) 分组数 = 102425

  —— 逐级对账 ——
  [PASS] 原始 items 行数 → 分组数                      实际=102425.00          期望=102425.00          行
  [PASS] 原始 Σprice → 转换 Σpay_amount             实际=13591643.70        期望=13591643.70        BRL

【3】DWD 层（dwd_order_detail）
  DWD 行数              = 102425
  Σ pay_amount（全部）  = 13591643.70 BRL
  Σ pay_amount（有效）  = 13494400.74 BRL
  唯一订单数（全部/有效）= 98666 / 98199
  dwd_clean_reject 行数 = 0
  [PASS] 转换 CSV 行数 → DWD 行数                     实际=102425.00          期望=102425.00          行
  [PASS] 转换 Σpay_amount → DWD Σpay_amount       实际=13591643.70        期望=13591643.70        BRL

【4】ADS 层（预计算结果）
  Σ ads_daily_trend.gmv = 13494400.74 BRL
  Σ ads_category_stat.gmv = 13494400.74 BRL
  ads_overview GMV      = 13494400.74 BRL
  ads_overview 下单量/有效订单量 = 98666.00 / 98199.00
  [PASS] DWD 有效 Σpay_amount → ADS 趋势 Σgmv       实际=13494400.74        期望=13494400.74        BRL
  [PASS] DWD 有效 Σpay_amount → ADS 类目 Σgmv       实际=13494400.74        期望=13494400.74        BRL
  [PASS] ADS 类目 Σgmv → ads_overview GMV         实际=13494400.74        期望=13494400.74        BRL
  [PASS] DWD 唯一订单数 → ads_overview 下单量           实际=98666.00           期望=98666.00           单
  [PASS] DWD 有效唯一订单 → ads_overview 有效订单量        实际=98199.00           期望=98199.00           单

【5】订单状态映射对照（原始 → 转换后）
  转换后状态分布：
    已完成         100196   （计入有效订单：是）
    已支付           1757   （计入有效订单：是）
    已取消            465   （计入有效订单：否）
    无法履约             7   （计入有效订单：否）

  逐状态映射明细（原始状态 → 转换后状态 → 依据）：
    delivered    n=96478  → 已完成      状态名 + 三个履约时间戳齐全
    shipped      n=1107   → 已支付      order_approved_at 覆盖率 100%
    invoiced     n=314    → 已支付      order_approved_at 覆盖率 100%
    processing   n=301    → 已支付      order_approved_at 覆盖率 100%
    approved     n=2      → 已支付      order_approved_at 覆盖率 100%
    created      n=5      → 待付款      order_approved_at 覆盖率 0% —— 付款未审批通过
    canceled     n=625    → 已取消      状态名直接表明取消
    unavailable  n=609    → 无法履约     已批准但未履约；原始数据无退款字段，不断言已退款

  ⚠ 退款率说明：原始数据中没有任何「退款」字段，
    转换后不存在「已退款」状态，因此 REFUND_RATE 在本数据源下恒为 0，
    它反映的是「数据源缺少退款标识」，不能解读为「真实退款率为 0」。

【6】排除规则与分母口径（★ 这一节决定所有比率的分母）
  orders 表订单数                     = 99441
  其中在商品明细表里出现的订单数        = 98666
  ★ 没有商品行、因此完全不进 DWD 的订单 = 775

  这 775 笔被排除的订单按状态分布：
    unavailable    603 笔（占该状态  99.0%）
    canceled       164 笔（占该状态  26.2%）
    created          5 笔（占该状态 100.0%）
    invoiced         2 笔（占该状态   0.6%）
    shipped          1 笔（占该状态   0.1%）

  ⚠ 因此：
    · 「下单量」的分子/分母都是 DWD 里有商品行的订单 = 98666，不是 orders 表的 99441；
    · 「退款率 = 退款订单量 ÷ 下单量」的分母同样是 98666；
    · 无商品行的订单（大多是 unavailable / canceled）不参与任何指标计算，
      这是数据源结构决定的，不是清洗丢弃 —— dwd_clean_reject 里没有它们。

  指标口径表（本轮实测值）：
    指标            分子                                 分母                      实测值
    GMV             Σ pay_amount，is_valid_order=1           —                         13494400.74 BRL
    下单量          COUNT(DISTINCT order_id)（DWD 全部）      —                         98666
    有效订单量      COUNT(DISTINCT order_id)，is_valid=1      —                         98199
    销售件数        Σ quantity，is_valid_order=1             —                         112101
    下单用户数      COUNT(DISTINCT user_id)，is_valid=1       —                         94983
    客单价          GMV                                   有效订单量                137.42 BRL
    件单价          GMV                                   销售件数
    退款率          退款订单量（本数据源无此标识）            下单量                    不适用（恒 0）
    折扣率          Σ discount_amount（本数据源恒 0）        Σ 原价金额                不适用（恒 0）

【7】两种「质量」必须分开看
  ① 转换后规则通过率 = 有效行数 ÷ 转换后行数
       = 102425 / 102425 = 1.000000
     它衡量的是「我们这 8 条规则在转换后的数据上有没有发现问题」，
     而不是原始数据的质量 —— 因为适配层已经先做过状态映射、类目补全、
     金额重算（pay_amount = Σprice），脏值在进入清洗前大半已被规范化。
  ② 原始数据质量：本脚本第 1 节给出的行数/金额即为原始事实，
     与官方公开统计一致（orders 99,441 / items 112,650 / Σprice 13,591,643.70 BRL），
     说明下载到的是完整、未被篡改的原始数据集。
  → 因此报告与文档里不应把「100% 通过率」表述成「原始数据质量 100%」。

======================================================================================
对账结果：PASS（全部 0 项通过）
  说明：GMV/件数/订单量为可加指标，逐级对账通过；
        用户数、客单价、件单价、退款率、折扣率为不可加指标，不参与跨层累计对账。
======================================================================================